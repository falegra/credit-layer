PORT=8080

# Use sslmode=disable for local/private network connections (app and DB on the same network)
# Use sslmode=require for cloud-hosted databases or connections over the internet
DATABASE_URL=postgres://user:password@localhost:5432/credit_layer?sslmode=disable
TEST_DATABASE_URL=postgres://user:password@localhost:5432/credit_layer_test?sslmode=disable
